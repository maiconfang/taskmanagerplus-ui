export class Task {
    id: number;
    title: string;
    description: string;
    dueDate : Date;
    completed: Boolean;
  }
  
  export class TaskFilter {
    title: string;
    description: string;
    dueDate : Date;
    completed: Boolean;
  }